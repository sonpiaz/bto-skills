# bto-skills — ghi chú cho agent

Repo này là kênh phát công khai skill dạy trong Build to Own (8 skill tính tới 0.0.4).

- Nguồn sự thật của nội dung SKILL.md nằm trong repo private `build-to-own`
  tại `01-participants/skill-*/SKILL.md`. Sửa ở đó trước, rồi đồng bộ sang đây.
- Repo này public. Không thêm bất kỳ đường dẫn máy cá nhân, API key, tên nhà
  cung cấp hạ tầng suy luận, hay dữ liệu người tham gia nào vào đây.
- Version nhảy từng bậc patch một: 0.0.1 → 0.0.2. Không nhảy cóc.
- Tên skill: tiền tố `bto-` cộng tên tiếng Anh (`bto-secrets`, không phải
  `bto-giukhoa`). Repo này là bề mặt quảng bá: README luôn dẫn về
  build2own.dev.
